# Historical debug sweep, 2026-06-29

This file used to list MCP-era bugs from the first implementation. The runtime architecture has since changed.

Current state:

- Agents call `kbs` through the shell.
- `server/search_core.py` holds the transport-free search logic.
- `server/cli.py` is the user-facing command surface.
- `server/state.py` stores context memory.
- The old adapter file was removed.

Use `tasks/bugs.md` for current reliability notes.
