[project]
name = "kbs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "bm25s~=0.3.9",
  "numpy~=2.5.1",
  "pypdf~=6.14.2",
]

[tool.pytest.ini_options]
markers = [
  "integration: requires live network access",
]
