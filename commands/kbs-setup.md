---
description: Install the kbs CLI and create its user configuration
allowed-tools: Bash
---

Run the plugin setup script:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh"
```

Then verify the installation:

```bash
kbs doctor
```
